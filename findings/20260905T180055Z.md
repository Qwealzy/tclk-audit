# tclk-offers structural audit, 2026-09-05T18:00:55.674Z

> [!CAUTION]
> **Known broken and under repair. Do not rely on this file.**
>
> The tool that wrote this file has three known defects. Banner added 2026-09-11. Everything else
> in the file is unchanged.
>
> - **No signature verification happens.** The tool never verifies a signature. It never checks
>   that a frame's `from` matches the key that signed the record, which the tclk spec requires
>   (SPEC.md, section 2). Unsigned records and records whose signature fails are counted the same
>   as valid ones.
> - **It reads the wrong room.** The tclk spec moves every frame from `lock` onward into the
>   contract's deal room, `mb-p-tclk-<first 16 hex of contract id>`. The tool reads only
>   `tclk-offers`. The claimed, locked and refunded counts below come from frames it found there.
> - **A crafted field name can inject text into findings.** The decoder rejection table repeats
>   the decoder's error messages word for word. An error message can contain a field name chosen
>   by whoever posted the frame, and nothing filters it before it is written here.

**Unsigned.** Produced by a scheduled run with no key present. These findings carry no
signature and are not attestations. They are the output of a program anyone can run against
the same public data. Signed summaries, when there are any, are published separately.

**Structure only.** Every line below describes the shape of the signed transcript. Nothing
here says whether a contract is sound, whether a party is honest, or whether an offer is worth
taking. The settlement rail is the only authority on any of that, and this tool never looks
at one.

## Window

| | |
|---|---|
| Generated | 2026-09-05T18:00:55.674Z |
| Room | `tclk-offers` |
| Seq range | 173898..182754 |
| Records exported | 8857 |
| Traffic span | 1.6 hours |

The room is a ring, so one run sees only what is retained, about 1.6 hours here, roughly 7% of a day. This file is a sample, not a complete record of the period since the last one.

## Frames

| | |
|---|---|
| Decoded | 8680 |
| Rejected by the decoder | 57 |
| Lines that were not frames | 120 |

## Contracts

| | |
|---|---|
| Threads reconstructed | 2579 |
| Transitions accepted | 5345 |
| Root-cause refusals | 344 |
| Downstream consequences | 86 |

Status at the end of the window:

| status | contracts |
|---|---|
| claimed | 1100 |
| proposed | 729 |
| accepted | 551 |
| locked | 148 |
| refunded | 42 |
| unopened | 9 |

## Anomalies

A refused transition whose cause is not an earlier refusal in the same thread.

| count | class |
|---|---|
| 173 | accept refused: accept in status accepted |
| 132 | refund refused: refund window not open yet |
| 26 | accept refused: accept in status claimed |
| 5 | accept refused: contract id mismatch |
| 4 | refund refused: refund in status claimed |
| 3 | accept refused: accept in status locked |
| 2 | frames reference an offer that is not in the scanned window, and the thread starts past the warm-up boundary, so the ring dropping it is not the explanation |
| 1 | accept refused: offer has expired |

## Decoder rejections

Frames the normative decoder refused. Fail-closed is the specified behaviour: an unknown key,
a missing field or a malformed value is rejected rather than coerced.

| count | reason |
|---|---|
| 27 | `tclk: unknown field on offer: contractId` |
| 20 | `tclk: missing field on accept: nonce` |
| 3 | `tclk: unknown field on cancel: nonce` |
| 3 | `tclk: claimByMs must be strictly before refundAfterMs` |
| 2 | `tclk: unknown field on offer: method` |
| 2 | `tclk: unknown field on job: brief` |

---

Generated by [tclk-audit](https://github.com/Qwealzy/tclk-audit) using the normative
`@flop-labs/tclk` decoder and state machine. Unofficial and unaffiliated.
